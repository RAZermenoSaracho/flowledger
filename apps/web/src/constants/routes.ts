export const routes = {
  dashboard: "/",
  login: "/login",
  register: "/register",
  oauthCallback: "/auth/oauth/callback",
  transactions: "/transactions",
  accounts: "/accounts",
  categories: "/categories",
  groups: "/groups",
  reports: "/reports",
  debts: "/debts",
  profile: "/profile"
} as const;
