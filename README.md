# flowledger

FlowLedger is a modern personal finance platform for tracking expenses, shared spending, accounts, and financial insights in one clean dashboard.

## AI-Assisted Workflow

This repository is prepared for future Codex work.

- `main` is the stable human-reviewed branch.
- `razs_ai` is the AI working branch.
- Codex should read `RULES.md`, `AGENTS.md`, and `ROADMAP.md` before coding.

Start Codex manually from this project directory:

```bash
codex
```
